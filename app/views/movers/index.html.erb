<h1>Listing movers</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Mobile phone</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th>Gmaps</th>
    <th>Address</th>
    <th>Range</th>
    <th>Availability</th>
    <th>Hourly rate</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @movers.each do |mover| %>
  <tr>
    <td><%= mover.name %></td>
    <td><%= mover.email %></td>
    <td><%= mover.mobile_phone %></td>
    <td><%= mover.latitude %></td>
    <td><%= mover.longitude %></td>
    <td><%= mover.gmaps %></td>
    <td><%= mover.address %></td>
    <td><%= mover.range %></td>
    <td><%= mover.availability %></td>
    <td><%= mover.hourly_rate %></td>
    <td><%= link_to 'Show', mover %></td>
    <td><%= link_to 'Edit', edit_mover_path(mover) %></td>
    <td><%= link_to 'Destroy', mover, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mover', new_mover_path %>
